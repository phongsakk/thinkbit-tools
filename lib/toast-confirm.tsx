"use client"

/** Re-export confirm via SweetAlert2 (blur backdrop). Success/error/loading stay on react-hot-toast. */
export { swalConfirm, toastConfirm } from "@/lib/swal"
export type { SwalConfirmOptions } from "@/lib/swal"
